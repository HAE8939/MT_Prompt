{
  "name": "@promptvault/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest src/app.test.ts src/modules/provider",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@fastify/multipart": "^9.2.1",
    "@fastify/static": "^10.1.3",
    "@prisma/client": "^6.14.0",
    "@promptvault/compiler": "*",
    "@promptvault/contracts": "*",
    "fastify": "^5.5.0",
    "fflate": "^0.8.3"
  },
  "devDependencies": {
    "@types/node": "^24.3.0"
  }
}
