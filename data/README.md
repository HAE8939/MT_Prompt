# Data

`imports/` contains source collections used by explicit, idempotent import commands. These files are not read during normal application startup.

The Ciyuan collection is imported with:

```bash
npm run import:ciyuan01
```
